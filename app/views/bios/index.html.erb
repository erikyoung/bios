
	<div class="hero text-center"></div><br />
		<div class="index_content">
			<h2 class>Awesome People are Hard To Find</h2>
			<p>Let's Name Them</p>
			<%= link_to 'Add a Cool Person', new_bio_path, class: 'btn btn-info btn-lg btn3d' %> 
		</div>
  </div> 
<br />

<h1 class="text-center">The Awesome Ones</h1>


  
<div class="container">
  <div class="row text-center">
  <% @bios.each do |bio| %>
    <div class="booyah-box col-md-4">
      <h1><%= link_to bio.name, bio_path(bio) %></h1>
    </div> 
    <% end %>
  </div>
</div>

<div class="container">
  <h1>Photos</h1>
  <br />

  <% @bios.photos.each do |photo| %> 
    <%= image_tag photo.picture %> 
    <br />
    <%= photo.caption %> 
    <br />
    <% end %> 
</div>
 
  


























