
<div class="booyah-box col-xs-10 col-xs-offset-1">
	<h1 class="heroes text-center">Heroes</h1>
	<%= @bios.each do |bio| %> 
		<%= bio.name %> 
		<%= bio.age %> 
		<%= bio.description %> 
		<%= bio.hometown %> 
		<%= bio.hobbies %> 
	<% end %> 
</div>